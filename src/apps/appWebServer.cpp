#include "AppManager.h"
static const uint8_t webserver_bits[] = {
    };
class AppWebserver : public AppBase
{
private:
    /* data */
public:
    AppWebserver()
    {
        name = "webserver";
        title = "网页配置";
        description = "通过网页配置此设备";
        image = webserver_bits;
        _showInList = true;
        _reentrant = false;
    }
    void setup();
};
static AppWebserver app;

void AppWebserver::setup()
{
    if (GUI::msgbox_yn("是否连接WiFi", "如果使用WiFi，选择“确定”如果使用SoftAP，选择“取消”", "WiFi(A)", "SoftAP(B)"))
    {
        display.clearScreen();
        u8g2Fonts.setCursor(0, 15);
        u8g2Fonts.setFontMode(1);
        u8g2Fonts.printf("将连接WiFi并启动HTTP服务器\n");
        display.display(true);
        hal.autoConnectWiFi();
        enableIPv6();
        beginWebServer();
        u8g2Fonts.printf("请在浏览器中打开以下网址\n");
        u8g2Fonts.printf("http://%s\n", WiFi.localIP().toString().c_str());
        u8g2Fonts.printf("按下右键重启\n");
        display.display(true);
        while (1)
        {
            updateWebServer();
            if (digitalRead(PIN_BUTTONR) == 0)
            {
                ESP.restart();
                break;
            }
        }
    }
    else
    {
        display.clearScreen();
        u8g2Fonts.setCursor(0, 15);
        u8g2Fonts.setFontMode(1);
        u8g2Fonts.printf("将启动临时SoftAP和HTTP服务器\n");
        u8g2Fonts.printf("请按照屏幕提示操作\n");
        u8g2Fonts.printf("期间无法操作此设备\n");
        u8g2Fonts.printf("10分钟后自动退出\n");
        display.display(false);
        delay(5000);
        hal.WiFiConfigManual();
        ESP.restart();
    }
}
